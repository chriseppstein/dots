# Local-development overrides for [vars] in wrangler.toml — used only by
# `wrangler dev`, never deployed. No secrets here; safe to commit.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001
